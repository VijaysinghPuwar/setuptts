# Exact versions the release is built and tested with.  Bump deliberately and
# re-run the full suite plus the packaged-build self-test.
#  - PySide6 6.10+ requires macOS 15 (its shiboken binaries are built for it,
#    whatever the wheel tag says); LSMinimumSystemVersion in setuptts.spec
#    must match — CI verifies this against the bundled binaries.
#  - edge-tts 7.x: SentenceBoundary offsets are compensated for CBR audio,
#    which the per-chunk completeness check relies on.
PySide6==6.11.0
edge-tts==7.2.8
aiohttp==3.13.3
platformdirs==4.9.4
certifi==2026.2.25
